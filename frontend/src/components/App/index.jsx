import { Layout } from 'components/Layout';
import React from 'react';
import { ChatScene } from 'scenes/Chat';

export const App = () => {
  return (
    <Layout>
      <ChatScene />
    </Layout>
  );
};
